import 'package:flutter/material.dart';

class DriverDetailView extends StatelessWidget {
  const DriverDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Driver Detail View "),
      ),
    );
  }
}